# Frontend environment variables (Vite)
# Copy this file to .env and adjust if you run the backend on a different port or host.

# URL of the Express backend (no trailing slash)
VITE_BACKEND_URL=http://localhost:3001
